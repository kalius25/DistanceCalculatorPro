# Sprint 1B.3 — Workspace Focus Mode

This increment removes duplicated sheet metadata, adds larger preview sizes, and allows the file-selection panels to be collapsed so the data preview can use more vertical space.
