# Sprint 2.5.2.3-A — Performance Regression Foundation

Adds approved benchmark baseline persistence, threshold policies, deterministic
PASS/WARNING/REGRESSION comparison, and JSON/Markdown regression reports.

Run the focused suite:

```powershell
pytest tests\benchmarks -m performance_regression -vv
```
